# Copy to .env and fill in. The value is read at build time by Vite
# (see envPrefix in vite.config.ts) and inlined into the webview bundle.
TYPESAFE_API_KEY=

# Optional overrides.
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_DEFAULT_MODEL=jev-latest
